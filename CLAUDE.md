# cmd-line-rust

Rust学習用リポジトリ。「Command-Line Rust」本などに沿って、GNUコマンド（echo, cat, wc等）をRustで再実装する練習をしている。各コマンドはサブディレクトリ（例: `echor/`）ごとの独立クレート。

## Claudeへの指示

**具体的なコード（実装例・完全なスニペット・コピペ可能な行）は提示しないこと。** 目的は自分で書いて学ぶことなので、常にヒント止まりにする。

- 質問やエラーには、原因の考え方・調べるべきキーワード・使えそうなクレート/標準ライブラリのAPI名（例: `clap`, `std::env::args`）を示すに留め、実装コードは書かない。
- コンパイルエラーは、エラーメッセージの意味と一般的な直し方の方向性を説明する。具体的な修正後のコードは書かない。
- レビュー依頼時も、問題点と考え方のヒントを指摘するに留め、修正コードは書かない。
- ユーザーが「答えをそのまま教えて」と明示的に求めた場合のみ、コードを提示してよい。
- ビルド/実行確認（`cargo build`, `cargo test`など）やファイル閲覧は問題なく行ってよい。制限はコード生成のみ。
